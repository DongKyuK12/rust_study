# 09장. 제네릭과 트레이트

> **목표**: 제네릭으로 유연한 코드를 작성하고, 트레이트로 공통 동작을 정의할 수 있다.

---

## 1. 제네릭 함수

두 정수 중 큰 값을 반환하는 함수를 만들어봅시다:

```rust
fn largest_i32(a: i32, b: i32) -> i32 {
    if a > b {
        return a;
    }
    b
}
```

잘 동작합니다. 그런데 `f64` 버전도 필요하면?

```rust
fn largest_f64(a: f64, b: f64) -> f64 {
    if a > b {
        return a;
    }
    b
}
```

코드가 **거의 똑같은데** 타입만 다릅니다. 이런 중복을 없애는 것이 **제네릭**입니다.

### 제네릭으로 바꾸기

```rust
fn largest<T: PartialOrd>(a: T, b: T) -> T {
    if a > b {
        return a;
    }
    b
}

fn main() {
    println!("{}", largest(10, 20));       // 20  (i32)
    println!("{}", largest(3.14, 2.71));   // 3.14 (f64)
    println!("{}", largest('z', 'a'));      // z   (char)
}
```

해석해봅시다:

```
fn largest<T: PartialOrd>(a: T, b: T) -> T
//        ─┬─ ──────┬──── ──┬─  ──┬─    ─┬─
//         │        │       │     │      └─ 반환 타입도 T
//         │        │       │     └─ 두 번째 매개변수도 T
//         │        │       └─ 첫 번째 매개변수 타입 T
//         │        └─ T는 비교 가능해야 함 (트레이트 바운드)
//         └─ 타입 파라미터 선언
```

- `<T>`: "어떤 타입 T를 받겠다"는 선언
- `T: PartialOrd`: "단, T는 크기 비교가 가능한 타입이어야 한다"

<details>
<summary><b>🔍 제네릭이 성능에 영향을 주나? (원리)</b></summary>

### 단형화 (Monomorphization)

Rust의 제네릭은 **런타임 비용이 없습니다 (zero-cost)**. 이유는 **단형화** 때문입니다.

컴파일러가 제네릭 함수를 사용한 곳을 찾아서, **각 타입별로 전용 함수를 자동 생성**합니다:

```rust
// 우리가 쓴 코드
fn largest<T: PartialOrd>(a: T, b: T) -> T { ... }

largest(10, 20);       // i32로 호출
largest(3.14, 2.71);   // f64로 호출
```

컴파일러가 내부적으로 만드는 코드:

```rust
// 컴파일러가 자동 생성
fn largest_i32(a: i32, b: i32) -> i32 { ... }
fn largest_f64(a: f64, b: f64) -> f64 { ... }
```

결과적으로 **우리가 직접 타입별로 함수를 만든 것과 동일한 성능**입니다.

| | 제네릭 | 직접 작성 |
|---|---|---|
| 코드 양 | 하나만 작성 | 타입별로 각각 작성 |
| 실행 속도 | 동일 | 동일 |
| 바이너리 크기 | 사용한 타입 수만큼 생성 | 동일 |

**정리**: 제네릭은 "개발자의 편의 기능"이며, 컴파일 후에는 직접 작성한 코드와 차이가 없습니다.

</details>

---

## 2. 제네릭 구조체와 열거형

### 제네릭 구조체

```rust
struct Point<T> {
    x: T,
    y: T,
}

fn main() {
    let int_point = Point { x: 5, y: 10 };        // Point<i32>
    let float_point = Point { x: 1.5, y: 3.7 };   // Point<f64>

    println!("({}, {})", int_point.x, int_point.y);
    println!("({}, {})", float_point.x, float_point.y);
}
```

`x`와 `y`가 **같은 타입 T**입니다. 다른 타입을 넣으면 에러가 납니다:

```rust
let p = Point { x: 5, y: 3.14 }; // ❌ x는 i32인데 y는 f64
```

### 여러 타입 파라미터

`x`와 `y`가 다른 타입이어야 한다면 타입 파라미터를 두 개 쓰면 됩니다:

```rust
struct Point<T, U> {
    x: T,
    y: U,
}

fn main() {
    let p = Point { x: 5, y: 3.14 }; // ✅ Point<i32, f64>
    println!("({}, {})", p.x, p.y);
}
```

### 이미 써본 제네릭

사실 제네릭은 이미 많이 사용해왔습니다:

```rust
let numbers: Vec<i32> = vec![1, 2, 3];           // Vec<T>에 T = i32
let name: Option<String> = Some("홍길동".into());  // Option<T>에 T = String
let result: Result<i32, String> = Ok(42);          // Result<T, E>에 T = i32, E = String
```

| 타입 | 의미 |
|------|------|
| `Vec<T>` | T 타입의 값을 담는 가변 배열 |
| `Option<T>` | T 타입의 값이 있거나(Some) 없거나(None) |
| `Result<T, E>` | 성공하면 T, 실패하면 E |

### 제네릭 구조체에 메서드 추가

```rust
struct Point<T> {
    x: T,
    y: T,
}

impl<T> Point<T> {
    fn new(x: T, y: T) -> Self {
        Point { x, y }
    }

    fn x(&self) -> &T {
        &self.x
    }
}

fn main() {
    let p = Point::new(10, 20);
    println!("x = {}", p.x());
}
```

`impl<T>`에서 `<T>`를 빼먹으면 안 됩니다. "이 impl 블록은 제네릭 타입 T를 사용한다"고 알려주는 것입니다.

---

## 3. 트레이트 정의와 구현

**트레이트(trait)**는 "이런 동작을 할 수 있다"는 약속입니다.

다른 언어와 비교하면:

| 언어 | 비슷한 개념 |
|------|-----------|
| Java | interface |
| TypeScript | interface |
| Go | interface |
| Python | ABC (추상 클래스) |

### 트레이트 정의하기

```rust
trait Greet {
    fn hello(&self) -> String;
}
```

`Greet` 트레이트를 구현하는 타입은 반드시 `hello` 메서드를 가져야 합니다.

### 트레이트 구현하기

```rust
struct User {
    name: String,
}

struct Bot {
    id: u32,
}

impl Greet for User {
    fn hello(&self) -> String {
        format!("안녕하세요, {}입니다!", self.name)
    }
}

impl Greet for Bot {
    fn hello(&self) -> String {
        format!("저는 Bot #{}입니다.", self.id)
    }
}

fn main() {
    let user = User { name: String::from("홍길동") };
    let bot = Bot { id: 42 };

    println!("{}", user.hello()); // 안녕하세요, 홍길동입니다!
    println!("{}", bot.hello());  // 저는 Bot #42입니다.
}
```

`User`와 `Bot`은 전혀 다른 구조체이지만, 둘 다 `Greet` 트레이트를 구현했으므로 `.hello()`를 호출할 수 있습니다.

### 기본 구현 (Default Implementation)

모든 구현체에서 동일한 동작이 있다면 **기본 구현**을 제공할 수 있습니다:

```rust
trait Greet {
    fn hello(&self) -> String;

    fn goodbye(&self) -> String {
        String::from("안녕히 가세요!") // 기본 구현
    }
}

struct User {
    name: String,
}

impl Greet for User {
    fn hello(&self) -> String {
        format!("안녕하세요, {}입니다!", self.name)
    }
    // goodbye()는 구현하지 않아도 됨 → 기본 구현 사용
}

fn main() {
    let user = User { name: String::from("홍길동") };
    println!("{}", user.hello());   // 안녕하세요, 홍길동입니다!
    println!("{}", user.goodbye()); // 안녕히 가세요!
}
```

기본 구현이 있으면 **구현하지 않아도 자동으로 사용**됩니다. 물론 원하면 덮어쓸 수도 있습니다.

---

## 4. 트레이트 바운드

제네릭 함수에서 "T는 아무 타입이나 되면 안 되고, 특정 동작을 할 수 있어야 해"라고 제한하는 것이 **트레이트 바운드**입니다.

### 기본 문법

```rust
use std::fmt::Display;

fn print_item<T: Display>(item: T) {
    println!("값: {}", item);
}

fn main() {
    print_item(42);        // ✅ i32는 Display 구현
    print_item("안녕");     // ✅ &str은 Display 구현
    print_item(3.14);      // ✅ f64는 Display 구현
}
```

`T: Display`는 "T는 Display 트레이트를 구현한 타입이어야 한다"는 뜻입니다.

### 여러 트레이트 바운드

`+`로 여러 트레이트를 동시에 요구할 수 있습니다:

```rust
use std::fmt::{Display, Debug};

fn print_detail<T: Display + Debug>(item: T) {
    println!("출력: {}", item);    // Display
    println!("디버그: {:?}", item); // Debug
}
```

### where 절

트레이트 바운드가 길어지면 `where`절로 깔끔하게 분리합니다:

```rust
// 바운드가 길어서 읽기 힘듦
fn process<T: Display + Clone + PartialOrd, U: Debug + Default>(a: T, b: U) { ... }

// where 절로 분리하면 깔끔
fn process<T, U>(a: T, b: U)
where
    T: Display + Clone + PartialOrd,
    U: Debug + Default,
{
    // ...
}
```

### impl Trait 문법 (간단한 경우)

매개변수가 하나뿐이고 간단한 경우, 더 짧게 쓸 수 있습니다:

```rust
// 이 두 함수는 동일한 의미입니다
fn print_item<T: Display>(item: T) { ... }
fn print_item(item: impl Display) { ... }  // 더 짧은 문법
```

반환 타입에도 사용 가능합니다:

```rust
fn make_greeting(name: &str) -> impl Display {
    format!("안녕하세요, {}님!", name)
}

fn main() {
    println!("{}", make_greeting("홍길동"));
}
```

> **언제 뭘 쓸까?**
> - 간단한 경우 → `impl Trait`
> - 같은 타입을 여러 곳에서 쓰거나 복잡한 경우 → `<T: Trait>` 또는 `where`

---

## 5. 주요 표준 트레이트

Rust 표준 라이브러리에는 자주 사용하는 트레이트들이 있습니다. 알아두면 매우 편리합니다.

### Display: 사용자용 출력

`println!("{}", x)`로 출력하려면 `Display` 트레이트가 필요합니다.

```rust
use std::fmt;

struct User {
    name: String,
    age: u32,
}

impl fmt::Display for User {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        write!(f, "{}({}세)", self.name, self.age)
    }
}

fn main() {
    let user = User {
        name: String::from("홍길동"),
        age: 20,
    };
    println!("{}", user); // 홍길동(20세)
}
```

### Debug: 디버깅용 출력

`println!("{:?}", x)`로 디버깅할 때 사용합니다. `#[derive(Debug)]`로 자동 생성 가능합니다.

```rust
#[derive(Debug)]
struct Point {
    x: f64,
    y: f64,
}

fn main() {
    let p = Point { x: 1.0, y: 2.5 };
    println!("{:?}", p);  // Point { x: 1.0, y: 2.5 }
    println!("{:#?}", p); // 줄바꿈 포함 예쁜 출력
}
```

### Clone과 Copy

```rust
// Clone: 명시적 복사 (.clone() 호출 필요)
#[derive(Debug, Clone)]
struct Config {
    name: String,
    value: i32,
}

// Copy: 암시적 복사 (대입만 해도 복사됨)
// Copy는 Clone이 필수이고, 힙 데이터가 없는 간단한 타입만 가능
#[derive(Debug, Clone, Copy)]
struct Point {
    x: f64,
    y: f64,
}

fn main() {
    let p1 = Point { x: 1.0, y: 2.0 };
    let p2 = p1;      // Copy가 있어서 복사됨
    println!("{:?}", p1); // ✅ p1 사용 가능

    let c1 = Config { name: String::from("test"), value: 1 };
    let c2 = c1.clone(); // Clone으로 명시적 복사
    // println!("{:?}", c1); // String이 있어서 Copy 불가, clone() 필수
}
```

| 트레이트 | 동작 | 조건 |
|---------|------|------|
| `Clone` | `.clone()`으로 복사 | 대부분 가능 |
| `Copy` | 대입 시 자동 복사 | 힙 데이터 없는 타입만 |

### From과 Into

타입 변환을 위한 트레이트입니다.

```rust
struct Celsius(f64);
struct Fahrenheit(f64);

impl From<Celsius> for Fahrenheit {
    fn from(c: Celsius) -> Self {
        Fahrenheit(c.0 * 9.0 / 5.0 + 32.0)
    }
}

fn main() {
    let boiling = Celsius(100.0);
    let f: Fahrenheit = Fahrenheit::from(boiling); // From 사용

    let body = Celsius(36.5);
    let f2: Fahrenheit = body.into(); // Into는 From을 구현하면 자동 제공

    println!("{}F", f.0);  // 212F
    println!("{}F", f2.0); // 97.7F
}
```

> **규칙**: `From`만 구현하면 `Into`는 자동으로 생깁니다. `From`을 구현하세요.

### PartialEq와 Eq

동등 비교(`==`, `!=`)를 위한 트레이트입니다.

```rust
#[derive(Debug, PartialEq)]
struct User {
    name: String,
    age: u32,
}

fn main() {
    let a = User { name: String::from("홍길동"), age: 20 };
    let b = User { name: String::from("홍길동"), age: 20 };
    let c = User { name: String::from("김철수"), age: 25 };

    println!("{}", a == b); // true
    println!("{}", a == c); // false
}
```

| 트레이트 | 용도 |
|---------|------|
| `PartialEq` | `==`, `!=` 비교 가능 |
| `Eq` | PartialEq + 모든 값이 자기 자신과 같음을 보장 (f64는 NaN 때문에 Eq 불가) |

<details>
<summary><b>🔍 derive 매크로가 하는 일 (원리)</b></summary>

### derive는 코드를 자동 생성한다

`#[derive(Debug)]`를 붙이면 컴파일러가 **Debug 트레이트 구현 코드를 자동으로 작성**해줍니다.

```rust
#[derive(Debug)]
struct Point {
    x: f64,
    y: f64,
}
```

컴파일러가 내부적으로 이런 코드를 생성합니다:

```rust
impl fmt::Debug for Point {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        f.debug_struct("Point")
            .field("x", &self.x)
            .field("y", &self.y)
            .finish()
    }
}
```

직접 작성할 필요 없이 `#[derive(...)]` 한 줄이면 됩니다.

### derive 가능한 주요 트레이트

| 트레이트 | derive 가능 | 조건 |
|---------|------------|------|
| `Debug` | ✅ | 모든 필드가 Debug 구현 |
| `Clone` | ✅ | 모든 필드가 Clone 구현 |
| `Copy` | ✅ | 모든 필드가 Copy 구현 + Clone 필수 |
| `PartialEq` | ✅ | 모든 필드가 PartialEq 구현 |
| `Eq` | ✅ | PartialEq + 모든 필드가 Eq 구현 |
| `Display` | ❌ | 직접 구현해야 함 (출력 형태가 다양하므로) |

### 여러 트레이트를 한 번에

```rust
#[derive(Debug, Clone, PartialEq)]
struct Config {
    name: String,
    value: i32,
}
```

쉼표로 구분해서 **여러 개를 동시에 derive** 할 수 있습니다.

</details>

### 표준 트레이트 요약

| 트레이트 | 용도 | derive |
|---------|------|--------|
| `Display` | `{}` 출력 | ❌ (직접 구현) |
| `Debug` | `{:?}` 디버그 출력 | ✅ |
| `Clone` | `.clone()` 복사 | ✅ |
| `Copy` | 자동 복사 | ✅ |
| `From/Into` | 타입 변환 | ❌ (직접 구현) |
| `PartialEq` | `==` 비교 | ✅ |
| `Eq` | 완전 동등성 보장 | ✅ |
| `PartialOrd` | `<`, `>` 비교 | ✅ |
| `Default` | 기본값 생성 | ✅ |

---

## 6. 실습

### 실습 1: 제네릭 max 함수 만들기

두 값 중 큰 값을 반환하는 `max_value` 함수를 만드세요.

**요구사항:**
- 제네릭 함수여야 합니다
- 정수, 실수, 문자 모두 동작해야 합니다

```rust
fn max_value<T: PartialOrd>(a: T, b: T) -> T {
    if a >= b {
        return a;
    }
    b
}

fn main() {
    println!("정수: {}", max_value(10, 20));        // 20
    println!("실수: {}", max_value(3.14, 2.71));    // 3.14
    println!("문자: {}", max_value('가', '나'));      // 나
}
```

**예상 결과:**
```
정수: 20
실수: 3.14
문자: 나
```

**도전**: 세 개의 값 중 최댓값을 반환하는 `max_of_three` 함수도 만들어보세요!

### 실습 2: Summary 트레이트 정의하고 여러 구조체에 구현

뉴스 기사와 블로그 포스트에 공통으로 `summarize` 메서드를 구현하세요.

```rust
trait Summary {
    fn summarize(&self) -> String;

    fn read_time(&self) -> u32 {
        1 // 기본 구현: 1분
    }
}

struct NewsArticle {
    title: String,
    author: String,
    content: String,
}

struct BlogPost {
    title: String,
    author: String,
    body: String,
}

impl Summary for NewsArticle {
    fn summarize(&self) -> String {
        format!("[뉴스] {} - {}", self.title, self.author)
    }
}

impl Summary for BlogPost {
    fn summarize(&self) -> String {
        format!("[블로그] {} by {}", self.title, self.author)
    }

    fn read_time(&self) -> u32 {
        (self.body.len() as u32) / 100 + 1 // 글자 수 기반 계산
    }
}

// 트레이트 바운드를 사용한 함수
fn print_summary(item: &impl Summary) {
    println!("{}", item.summarize());
    println!("  예상 읽기 시간: {}분", item.read_time());
}

fn main() {
    let article = NewsArticle {
        title: String::from("Rust 2025 에디션 발표"),
        author: String::from("김기자"),
        content: String::from("Rust 재단이 새로운 에디션을 발표했습니다..."),
    };

    let post = BlogPost {
        title: String::from("Rust로 웹서버 만들기"),
        author: String::from("박개발"),
        body: String::from("오늘은 Rust로 간단한 웹서버를 만들어보겠습니다. ".repeat(10).as_str().to_string()),
    };

    print_summary(&article);
    println!("---");
    print_summary(&post);
}
```

**예상 결과:**
```
[뉴스] Rust 2025 에디션 발표 - 김기자
  예상 읽기 시간: 1분
---
[블로그] Rust로 웹서버 만들기 by 박개발
  예상 읽기 시간: 5분
```

### 실습 3: Display 트레이트 구현하여 커스텀 출력

`Color` 구조체에 `Display` 트레이트를 구현하여 `#{RRGGBB}` 형태로 출력하세요.

```rust
use std::fmt;

#[derive(Debug)]
struct Color {
    r: u8,
    g: u8,
    b: u8,
}

impl Color {
    fn new(r: u8, g: u8, b: u8) -> Self {
        Color { r, g, b }
    }
}

impl fmt::Display for Color {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        write!(f, "#{:02X}{:02X}{:02X}", self.r, self.g, self.b)
    }
}

fn main() {
    let red = Color::new(255, 0, 0);
    let green = Color::new(0, 255, 0);
    let white = Color::new(255, 255, 255);

    // Display ({})와 Debug ({:?}) 비교
    println!("빨강: {}", red);     // #FF0000
    println!("초록: {}", green);   // #00FF00
    println!("흰색: {}", white);   // #FFFFFF
    println!("디버그: {:?}", red); // Color { r: 255, g: 0, b: 0 }
}
```

**예상 결과:**
```
빨강: #FF0000
초록: #00FF00
흰색: #FFFFFF
디버그: Color { r: 255, g: 0, b: 0 }
```

**도전**: `From<(u8, u8, u8)> for Color`도 구현하여 `Color::from((255, 0, 0))` 형태로 생성할 수 있게 만들어보세요!

---

## 7. 확인 문제

### 문제 1

아래 코드가 컴파일되지 않는 이유는?

```rust
fn add<T>(a: T, b: T) -> T {
    a + b
}
```

### 문제 2

`#[derive(Debug, Clone, PartialEq)]`는 무엇을 하는 코드인가요?

### 문제 3

아래 빈칸을 채우세요:

```rust
trait Area {
    fn area(&self) -> f64;
}

struct Circle {
    radius: f64,
}

impl ______ for Circle {
    fn area(&self) -> f64 {
        3.14159 * self.radius * self.radius
    }
}
```

### 문제 4

아래 두 함수 시그니처의 차이점은?

```rust
fn print_a<T: Display>(item: T) { ... }
fn print_b(item: impl Display) { ... }
```

### 문제 5

`From`과 `Into` 트레이트의 관계를 설명하세요.

---

## 확인 문제 정답

<details>
<summary>정답 보기 (먼저 풀어본 후 클릭하세요!)</summary>

### 문제 1 정답

`T`에 **트레이트 바운드가 없기 때문**입니다. `+` 연산을 사용하려면 `Add` 트레이트가 필요합니다.

수정:
```rust
use std::ops::Add;

fn add<T: Add<Output = T>>(a: T, b: T) -> T {
    a + b
}
```

`T: Add<Output = T>`는 "T 타입끼리 더했을 때 결과도 T 타입이어야 한다"는 뜻입니다.

### 문제 2 정답

컴파일러가 `Debug`, `Clone`, `PartialEq` 트레이트의 구현 코드를 **자동으로 생성**합니다.
- `Debug`: `{:?}`로 디버그 출력 가능
- `Clone`: `.clone()`으로 복사 가능
- `PartialEq`: `==`, `!=`로 비교 가능

### 문제 3 정답

```rust
impl Area for Circle {
```

`Area` 트레이트를 `Circle` 구조체에 구현하는 것이므로 `impl Area for Circle`입니다.

### 문제 4 정답

**기능적으로 동일합니다.** 둘 다 `Display`를 구현한 타입을 받습니다.

차이점:
- `<T: Display>`: 타입 파라미터 `T`를 명시적으로 선언. 여러 매개변수에서 같은 타입을 강제할 수 있음
- `impl Display`: 더 짧은 문법. 매개변수가 하나뿐인 간단한 경우에 편리

```rust
// <T>는 두 매개변수가 같은 타입임을 강제
fn compare<T: Display>(a: T, b: T) { ... }

// impl은 각각 다른 타입이어도 됨
fn compare(a: impl Display, b: impl Display) { ... }
```

### 문제 5 정답

`From`을 구현하면 `Into`는 **자동으로 제공**됩니다.

```rust
impl From<A> for B { ... }  // From만 구현하면
let b: B = a.into();        // Into는 자동으로 사용 가능
```

따라서 **항상 `From`을 구현**하면 됩니다. `Into`를 직접 구현할 필요는 없습니다.

</details>

---

## 8. 9장 정리

| 배운 것 | 핵심 |
|---------|------|
| 제네릭 함수 | `fn foo<T>(a: T)` - 여러 타입에 동작하는 함수 |
| 제네릭 구조체 | `struct Point<T> { x: T, y: T }` |
| 트레이트 정의 | `trait Greet { fn hello(&self); }` |
| 트레이트 구현 | `impl Greet for User { ... }` |
| 기본 구현 | 트레이트 메서드에 기본 동작 제공 |
| 트레이트 바운드 | `T: Display` - T가 가져야 할 능력 지정 |
| where 절 | 복잡한 바운드를 깔끔하게 작성 |
| impl Trait | `fn foo(item: impl Display)` - 짧은 바운드 문법 |
| Display | `{}` 출력용, 직접 구현 필요 |
| Debug | `{:?}` 디버그 출력, derive 가능 |
| Clone / Copy | 값 복사 (명시적 / 암시적) |
| From / Into | 타입 변환, From만 구현하면 Into는 자동 |
| PartialEq | `==` 비교, derive 가능 |
| derive | 트레이트 구현 코드 자동 생성 |

---

## 다음 장 예고

> **10장. 라이프타임 심화**에서는 참조의 유효 기간을 명시하는 방법을 배웁니다.
> 라이프타임 어노테이션(`'a`)으로 컴파일러에게 참조의 수명을 알려주고,
> 라이프타임 생략 규칙을 이해하면 코드가 훨씬 깔끔해집니다!
